MODULE IOTest;

IMPORT Out;

VAR
  a, i :INTEGER;

PROCEDURE Neat(x :INTEGER);
BEGIN
  Out.Char(CHR(x))
END Neat;

BEGIN
  Out.Char(65X);
  i := 65;
  WHILE i < 91 DO
    Neat(i);
    i := i + 1
  END;
  Out.Ln;
  ReadInt(a);
  a := a + 1;
  Out.Int(a);
  Out.Ln
END IOTest.
